{
  "name": "api-impulse",
  "version": "0.1.0",
  "description": "Monitor open source de disponibilidade, status e latência de APIs.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check src/app.js && node --check src/monitor.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
